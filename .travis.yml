addons:
  apt:
    packages:
      - cmake-data
      - cmake
      - libfreeimage3
      - libfreeimage-dev 
      - libboost-filesystem1.58
      - libboost-program-options1.58
      - clang

script:
  - cmake .
  - make
